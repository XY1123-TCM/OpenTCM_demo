import ChatView from '../../sections/chat/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Chat with OpenTCM',
};

export default function Page() {
  return <ChatView />;
}
