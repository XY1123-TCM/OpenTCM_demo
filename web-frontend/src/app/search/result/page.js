import SearchView from 'src/sections/search/view';
import SearchResults from 'src/sections/search/searchResults';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Search Results:',
};

export default function Page() {
  return <SearchResults />;
}
